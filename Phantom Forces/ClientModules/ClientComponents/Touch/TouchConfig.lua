
-- Decompiled with the Synapse X Luau decompiler.

return {
	buttonTypes = {
		ShootDragger = shared.require("TouchInputShootDragger"), 
		Movement = shared.require("TouchInputMovement"), 
		Shoot = shared.require("TouchInputShoot"), 
		Look = shared.require("TouchInputLook"), 
		ToggleSight = shared.require("TouchInputToggleSight"), 
		Firemode = shared.require("TouchInputFiremode"), 
		SwapGun = shared.require("TouchInputSwapGun"), 
		Inspect = shared.require("TouchInputInspect"), 
		Reload = shared.require("TouchInputReload"), 
		Sprint = shared.require("TouchInputSprint"), 
		Crouch = shared.require("TouchInputCrouch"), 
		Melee = shared.require("TouchInputMelee"), 
		Prone = shared.require("TouchInputProne"), 
		Jump = shared.require("TouchInputJump"), 
		Spot = shared.require("TouchInputSpot"), 
		Aim = shared.require("TouchInputAim"), 
		Aim2 = shared.require("TouchInputAim2")
	}
};

