# LUA Api for shambles haxx.

Cheat table
---
getgenv().cheat
---
    print(text (string), color3)
    ---
    notify(text (string), time (int), warning (bool))
    ---
    username
    ---
    uid
    ---
    library
    ---
    window
    ---
    tab